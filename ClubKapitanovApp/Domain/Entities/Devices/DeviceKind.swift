import Foundation

/// Тип физического устройства, которое может быть связано со сменой.
///
/// Операционное приложение работает только на iPad, поэтому доменная модель не
/// держит заготовок под мобильные или административные сценарии.
enum DeviceKind: String, Codable, Sendable, CaseIterable {
    case ipad
}
